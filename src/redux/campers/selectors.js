export const selectCampers = (state) => state.camper.items;

export const selectFavoritesCampers = (state) => state.camper.favoritesItems;

export const selectCurrentCump = (state) => state.camper.currentCamp;
